/**
 * Expanded chat panel. Renders answers, refusals as a distinct block rather than an error, and the
 * inline sign-in interstitial that holds the pending question and replays it after Clerk returns.
 * Focus trap, Esc closes, WCAG AA contrast.
 */
export default function AskPanel() {
  return null;
}
